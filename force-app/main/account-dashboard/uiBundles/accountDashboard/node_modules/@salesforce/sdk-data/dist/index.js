import { wrapSDKPromise as v, getSurface as C, Surface as l } from "@salesforce/sdk-core";
function i(t) {
  return P(t) ? t.then((e) => e) : {
    then: (e, r) => {
      try {
        return i(e(t));
      } catch (n) {
        return e === void 0 ? i(t) : w(n);
      }
    }
  };
}
function w(t) {
  return P(t) ? t.then((e) => e) : {
    then: (e, r) => {
      if (typeof r == "function")
        try {
          return i(r(t));
        } catch (n) {
          return w(n);
        }
      return w(t);
    }
  };
}
function P(t) {
  return typeof t?.then == "function";
}
function E(t = {
  request: [],
  retry: void 0,
  response: [],
  finally: []
}, e) {
  return {
    type: "fetch",
    version: "1.0",
    service: function(...r) {
      var n;
      const s = (n = t.createContext) == null ? void 0 : n.call(t), {
        request: a = [],
        retry: c = void 0,
        response: u = [],
        finally: d = []
      } = t, p = a.reduce(
        (o, h) => o.then((y) => h(y, s)),
        i(r)
      );
      return Promise.resolve(p).then((o) => c ? c(o, e, s) : e ? e.applyRetry(() => fetch(...o)) : fetch(...o)).then((o) => u.reduce(
        (h, y) => h.then((R) => y(R, s)),
        i(o)
      )).finally(() => {
        if (d.length > 0)
          return d.reduce(
            (o, h) => o.then(() => h(s)),
            Promise.resolve()
          );
      });
    }
  };
}
function f(t, e, [r, n = {}], {
  throwOnExisting: s = !1,
  errorMessage: a = `Unexpected ${t} header encountered`
} = {}) {
  let c = !1;
  if (r instanceof Request && !n?.headers) {
    if (s && r.headers.has(t))
      throw new Error(a);
    r.headers.set(t, e), c = !0;
  }
  if (n?.headers instanceof Headers) {
    if (s && n.headers.has(t))
      throw new Error(a);
    n.headers.set(t, e);
  } else {
    if (s && n?.headers && Reflect.has(n.headers, t))
      throw new Error(a);
    c || (n.headers = {
      ...n?.headers,
      [t]: e
    });
  }
  return [r, n];
}
function k(t) {
  return (e) => {
    const [r, n] = e;
    if (typeof r == "string" && !r.startsWith("http")) {
      const s = r.startsWith("/") ? r : `/${r}`;
      return i([`${t}${s}`, n]);
    }
    return i(e);
  };
}
function D(t) {
  return (e) => i(f("Authorization", `Bearer ${t}`, e));
}
function I(t, e) {
  const r = [];
  return t && r.push(k(t)), e && r.push(D(e)), E({
    request: r
  }).service;
}
const A = typeof __SF_API_VERSION__ < "u" ? __SF_API_VERSION__ : "65.0";
function T(t = A) {
  return `/services/data/v${t}`;
}
class g {
  clientFetch;
  pathData;
  constructor(e) {
    const r = $(), n = b(e?.instanceUrl ?? r.instanceUrl), s = e?.accessToken ?? r.accessToken;
    this.pathData = T(e?.apiVersion ?? r.apiVersion), this.clientFetch = I(n || void 0, s);
  }
  async graphql({
    query: e,
    variables: r,
    operationName: n
  }) {
    return (await this.clientFetch(`${this.pathData}/graphql`, {
      method: "POST",
      body: JSON.stringify({ query: e, variables: r, operationName: n }),
      headers: {
        "Content-Type": "application/json",
        Accept: "application/json"
      }
    })).json();
  }
}
function $() {
  const t = globalThis.MOSAIC_ENV;
  return {
    instanceUrl: t?.instanceUrl,
    accessToken: t?.accessToken,
    apiVersion: t?.apiVersion
  };
}
function b(t) {
  if (!t || t === "/") return "";
  let e = t;
  return !e.startsWith("/") && !e.startsWith("http") && (e = `/${e}`), e.endsWith("/") && (e = e.slice(0, -1)), e;
}
const M = "graphqlQuery";
class U {
  async graphql({
    query: e,
    variables: r,
    operationName: n
  }) {
    return (await window.openai.callTool(M, {
      query: e,
      ...r != null ? { variables: r } : {},
      ...n != null ? { operationName: n } : {}
    })).structuredContent;
  }
}
class q {
  constructor(e) {
    this.defaultRetryPolicy = e;
  }
  applyRetry(e, r) {
    return this.retry(e, r || this.defaultRetryPolicy);
  }
  async retry(e, r) {
    const n = Date.now();
    let s = 0, a = await e(), c = {
      attempt: s,
      totalElapsedMs: Date.now() - n,
      lastResult: a
    };
    for (; await r.shouldRetry(a, c); ) {
      const u = await r.calculateDelay(a, c);
      await this.delay(u), r.prepareRetry && await r.prepareRetry(a, c), s++, a = await e(), c = {
        attempt: s,
        totalElapsedMs: Date.now() - n,
        lastResult: a
      };
    }
    return a;
  }
  delay(e) {
    return new Promise((r) => {
      setTimeout(r, e);
    });
  }
}
class F {
}
function N(t) {
  return {
    version: "1.0",
    service: new q(t),
    type: "retry"
  };
}
const O = "X-SFDC-Client-Name", H = "X-SFDC-Client-Version", V = "@salesforce/sdk-data", W = "1.135.0", j = (t) => {
  let e = f(O, V, t);
  return e = f(H, W, e), i(e);
}, K = "X-CSRF-Token";
function L(t, e = {}) {
  const { protectedUrls: r = [], alwaysProtectedUrls: n = [] } = e;
  return async (s) => {
    const [a, c] = s, u = new Request(a, c);
    if (S(n, u.url) || x(u.method) && S(r, u.url)) {
      const p = await t.getToken();
      s = f(K, p, s);
    }
    return i(s);
  };
}
function x(t) {
  const e = t.toLowerCase();
  return e === "post" || e === "put" || e === "patch" || e === "delete";
}
function S(t, e) {
  const r = new URL(e);
  return t.some((n) => r.pathname.includes(n));
}
function B(t, e = {}) {
  const r = L(t, e);
  async function n(s) {
    const a = await r(s);
    return fetch(a[0], a[1]);
  }
  return (s, a) => a ? a.applyRetry(async () => n(s)) : n(s);
}
const z = [400, 401, 403];
class X extends F {
  constructor(e) {
    super(e), this.csrfTokenManager = e;
  }
  /**
   * Determines if a failed request should be retried due to CSRF token issues.
   */
  async shouldRetry(e, r) {
    return r.attempt >= 1 ? !1 : z.includes(e.status);
  }
  /**
   * CSRF token refresh should happen immediately with no delay.
   */
  async calculateDelay(e, r) {
    return 0;
  }
  /**
   * Called by retry service before each retry attempt.
   *
   * @param _result - The failed response that triggered the retry (unused, already validated)
   * @param _context - Current retry context (unused but part of interface)
   */
  async prepareRetry(e, r) {
    await this.csrfTokenManager.refreshToken();
  }
}
class J {
  constructor(e, r) {
    this.endpoint = e, this.cacheName = r, this.tokenPromise = this.obtainToken();
  }
  tokenPromise;
  refreshPromise;
  /**
   * Returns the current token value as a Promise.
   * Lazy-loads the token on first call (from cache or by fetching).
   */
  async getToken() {
    return this.tokenPromise;
  }
  /**
   * Obtains and returns a new token value as a promise.
   * This will clear the cached token and fetch a fresh one.
   * If a refresh is already in progress, all callers share the same in-flight promise.
   */
  refreshToken() {
    return this.refreshPromise || (this.refreshPromise = this.withCache((e) => e.delete(this.endpoint)).then(() => (this.tokenPromise = this.obtainToken(), this.tokenPromise)).finally(() => {
      this.refreshPromise = void 0;
    })), this.refreshPromise;
  }
  /**
   * Obtains a CSRF token, using cache when available or fetching a new one.
   *
   * @returns Promise that resolves to the CSRF token string
   */
  async obtainToken() {
    let e = await this.withCache((s) => s.match(this.endpoint)), r = !1;
    e || (e = await fetch(this.endpoint, { method: "get" }), r = !0);
    const n = (await e.clone().json()).csrfToken;
    return r && await this.withCache((s) => s.put(this.endpoint, e)), n;
  }
  /**
   * Provides a safe way to interact with the Cache API with fallback for unsupported environments.
   *
   * @param callback - Function that receives the cache instance and returns a promise
   * @returns The result of the callback, or undefined if caches API is not available
   */
  async withCache(e) {
    if (this.cacheName && caches) {
      const r = await caches.open(this.cacheName);
      return e(r);
    } else
      return;
  }
}
const m = /* @__PURE__ */ new Map();
function Q(t) {
  const { endpoint: e, cacheName: r, ...n } = t.csrf;
  let s = m.get(e);
  return s || (s = new J(e, r), m.set(e, s)), E(
    {
      retry: B(s, n),
      request: [j]
    },
    N(new X(s)).service
  ).service;
}
const Y = 1, G = `@salesforce/sdk-data_v${Y}`, _ = T(), Z = `${_}/ui-api`;
class ee {
  baseUrl;
  clientFetch;
  onStatus;
  constructor(e) {
    const r = te();
    this.baseUrl = re(e?.basePath ?? r.apiPath), this.onStatus = e?.onStatus ?? {}, this.clientFetch = Q({
      csrf: {
        endpoint: `${this.baseUrl}${Z}/session/csrf`,
        cacheName: G,
        protectedUrls: ["services/data/v", "services/apexrest"],
        alwaysProtectedUrls: ["services/apexrest"]
      }
    });
  }
  async graphql({
    query: e,
    variables: r,
    operationName: n
  }) {
    return (await this.fetch(`${_}/graphql`, {
      method: "POST",
      body: JSON.stringify({ query: e, variables: r, operationName: n }),
      headers: {
        "Content-Type": "application/json",
        Accept: "application/json",
        "X-Chatter-Entity-Encoding": "false"
      }
    })).json();
  }
  async fetch(e, r) {
    const n = this.applySalesforceBase(e), s = await this.clientFetch(n, r);
    return await this.onStatus[s.status]?.(), s;
  }
  /**
   * If the url is relative, convert to it to an absolute Salesforce URL.  This is due to the way Code
   * Builder deployments structure the url for Salesforce
   */
  applySalesforceBase(e) {
    if (typeof e == "string") {
      if (e.startsWith("http"))
        return e;
      const r = e.startsWith("/") ? e : `/${e}`;
      return `${this.baseUrl}${r}`;
    }
    return e;
  }
}
function te() {
  return {
    apiPath: globalThis.SFDC_ENV?.apiPath
  };
}
function re(t) {
  if (!t || t === "/") return "";
  let e = t;
  return e.startsWith("/") || (e = `/${e}`), e.endsWith("/") && (e = e.slice(0, -1)), e;
}
function se(t, ...e) {
  let r = t[0] ?? "";
  for (let n = 0; n < e.length; n += 1)
    r += String(e[n]) + (t[n + 1] ?? "");
  return r;
}
function ae(t) {
  return v(
    (async () => {
      switch (C(t?.surface)) {
        case l.OpenAI:
          return new U();
        case l.WebApp:
        case l.MicroFrontend:
          return new ee(t?.webapp);
        case l.Mosaic:
          return new g(t?.mosaic);
        case l.SalesforceACC:
        case l.MCPApps:
          return {};
        default:
          return {};
      }
    })(),
    "createDataSDK"
  );
}
export {
  ae as createDataSDK,
  se as gql
};
